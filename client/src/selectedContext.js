import { createContext } from "react";

export const selectedPostContext = createContext(null);
export const selectedPartContext = createContext(null);
export const MapCoordContext = createContext(null);